% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detectAnomalies.R
\name{detect_anomalies}
\alias{detect_anomalies}
\title{Detect Anomalies}
\usage{
detect_anomalies(
  num_trees = 500,
  mtry = 225,
  lambdas = 10^seq(3, -3, length = 100),
  omic_data_path = "",
  clinic_data_path = "",
  omic_predictors_to_ignore = NULL,
  clinic_predictors_to_ignore = NULL,
  id_column = NULL,
  active_predictors = NULL,
  numeric_active_predictors = NULL,
  categoric_active_predictors = NULL,
  class_variable = NULL,
  saving_name = "",
  n_cores = 6,
  partition_percentage = 0.9,
  diagnostic_change_probability = 0.1,
  seed = 1234,
  odds_default_values = character(0),
  weights = numeric(0),
  c = c(1),
  gamma = c(1),
  kernel = "lineal",
  just_analysis = FALSE,
  enrichment = "transcriptomic",
  absolute_path = "",
  omic = "",
  domain_table = NULL
)
}
\arguments{
\item{num_trees}{Integer | Number of trees of the Random Forest model. Default value: 100.}

\item{mtry}{Integer | Number of predictors evaluated at each node of each tree. Default value: 225.}

\item{lambdas}{Numeric vector | Sequence of lambda values for Lasso regression. Default value: 10^seq(3, -3, length = 100).}

\item{omic_data_path}{String | Path to omic data. If not specified, sample data will be used.}

\item{clinic_data_path}{String | Path to clinical data. If not specified, sample data will be used.}

\item{omic_predictors_to_ignore}{Character vector | Variables to remove from the omic dataset.}

\item{clinic_predictors_to_ignore}{Character vector | Variables to remove from the clinical dataset.}

\item{id_column}{String | Identifier variable in both datasets. Default: "Trial" (for sample data).}

\item{active_predictors}{Character vector | Predictors for ratio study after genetic algorithm execution.}

\item{numeric_active_predictors}{Character vector | Numerical predictors among active predictors.}

\item{categoric_active_predictors}{Character vector | Categorical predictors among active predictors.}

\item{class_variable}{String | Target variable, binary with two possible values. Default: "Ca.Co.Last" (for sample data).}

\item{saving_name}{String | Name for saving the model and solution. Default: Current date as string.}

\item{n_cores}{Integer | Number of cores for parallelization. Default value: 6.}

\item{partition_percentage}{Numeric | Fraction of data split into training and test sets. Default value: 0.9 (90%).}

\item{diagnostic_change_probability}{Numeric | Probability of each gene in solutions to change. Default value: 0.1 (10%).}

\item{seed}{Integer | Random seed for reproducibility. Default value: 1234.}

\item{odds_default_values}{Character vector | Default values for categorical features, formatted as "feature:value".}

\item{weights}{Numeric vector | Weights assigned to transcriptomics predictors.}

\item{c}{Numeric vector | Cost parameter for SVM. Default value: c(1).}

\item{gamma}{Numeric vector | Gamma parameter for SVM. Default value: c(1).}

\item{kernel}{String | Kernel type for SVM. Default value: "lineal".}

\item{just_analysis}{Logical | If TRUE, only analysis is performed without modifications.}

\item{enrichment}{String | Type of enrichment analysis. Default value: "transcriptomic".}

\item{absolute_path}{String | Absolute path for file outputs.}

\item{omic}{String | Omic type to be analyzed.}

\item{domain_table}{Data frame | Domain table used for enrichment analysis.}
}
\description{
Methodology based on the combination of a genetic algorithm and a Machine Learning technique to mutate the final
diagnosis of patients, detecting anomalies in them.
}
\details{
These discrepancies may have their origin in the evolution of the subject itself, leading it from one group to
another in biological terms, or they may derive from human error in the labeling of the samples, among other causes.

The identification and correction of these erroneous situations becomes essential to preserve the integrity
and accuracy of the data.
}
\examples{
detect_anomalies(saving_name = "DefaultExecutionLasso")
detect_anomalies(saving_name = "DefaultExecutionRF")
detect_anomalies(saving_name = "QuickExecution", active_predictors = c("sex", "age", "mutation", "ethnicity"))
detect_anomalies(saving_name = "ExecutionWithOwnData", omic_data_path = "./myOmicData.tsv", clinic_data_path = "./myClinicData.tsv", id_column = "patient_id", class_variable = "diagnosis", active_predictors = c("sex", "age", "ethnicity"), numeric_active_predictors = c("age"), categoric_active_predictors = c("sex", "ethnicity"))
#### Data Reading ####
#### Relevel Clinic Data ####
}
